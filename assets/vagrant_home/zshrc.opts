# copied from /vagrant/kevin/setup/zshrc.opts
# NOREPEAT

# set default editor
export EDITOR="vi"

# get rid of that awful green +x dir color
eval $( dircolors #SETUPDIR#/assets/vagrant_home/lscolors.txt )

# respect $LS_COLORS when auto-completing (tab)
zstyle ':completion:*:default' list-colors ${(s.:.)LS_COLORS}
